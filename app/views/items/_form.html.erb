<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :desc %>
    <%= f.text_field :desc %>
  </div>

  <div class="field">
    <%= f.label :todo_flag %>
    <%= f.check_box :todo_flag %>
  </div>

  <div class="field">
    <%= f.label :list_id %>
    <%= f.text_field :list_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
